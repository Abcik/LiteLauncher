# Installer module

This module contains install path and OS integration logic:

- LiteLauncher directory creation;
- bootstrap placement;
- icon placement;
- Windows/macOS/Linux shortcut/script creation.

The official installer pixel UI and embedded official `Bootstrap.jar` payload are redacted.
